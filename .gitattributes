*.pdf filter=lfs diff=lfs merge=lfs -text

# Translated study editions are generated text PDFs of ~60 KB, against the
# scanned originals' 4-15 MB. Keeping them out of LFS means they are served by
# raw.githubusercontent.com and never touch the LFS bandwidth quota.
translations/**/*.pdf !text !filter !merge !diff
